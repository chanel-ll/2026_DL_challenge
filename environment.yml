name: dcm_all
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch==2.4.0
      - transformers==4.48.3
      - vllm==0.6.3.post1

      - peft==0.14.0
      - trl==0.15.2
      - accelerate==0.34.2
      - deepspeed==0.15.4
      - bitsandbytes
      - liger-kernel<0.6

      - pandas
      - numpy<2
      - tensorboard
